.venv/
__pycache__/
*.pyc
.pytest_cache/
.pytest_tmp/
*.egg-info/
data/learning.db
data/backups/*.db
.DS_Store
# Internal agent work-memory notes -- not product source, may contain
# session-internal context. Was accidentally committed once (see git log);
# this stops any further files under here from being tracked. Existing
# committed history is left untouched pending an explicit decision from
# the repo owner on whether to rewrite it.
.workbuddy/
